apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: podinfo
  namespace: default
spec:
  interval: 1h0m0s
  chart:
    spec:
      chart: podinfo
      version: '6.9.2'
      sourceRef:
        kind: HelmRepository
        name: podinfo
        namespace: flux-system
      interval: 2h0m0s
