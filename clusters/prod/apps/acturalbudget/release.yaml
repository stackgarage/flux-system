apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: actualbudget
  namespace: default

spec:
  interval: 5m
  releaseName: actualbudget

  chart:
    spec:
      chart: unichart
      version: "0.0.5"
      sourceRef:
        kind: HelmRepository
        name: tinycloud-labs
        namespace: flux-system
      interval: 1m

  valuesFrom:
    - kind: ConfigMap
      name: actualbudget-values
      valuesKey: values.yaml
