apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: speedtest
  namespace: default

spec:
  interval: 5m
  releaseName: speedtest

  chart:
    spec:
      chart: unichart
      version: "0.0.4"
      sourceRef:
        kind: HelmRepository
        name: tinycloud-labs
        namespace: flux-system
      interval: 1m

  valuesFrom:
    - kind: ConfigMap
      name: speedtest-values
      valuesKey: values.yaml
