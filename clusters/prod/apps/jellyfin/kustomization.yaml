resources:
  - release.yaml

configMapGenerator:
  - name: app-values
    files:
      - app-values.yaml=app-values.yaml
  - name: media-values
    files:
      - media-values.yaml=media-values.yaml
  - name: config-values
    files:
      - config-values.yaml=config-values.yaml

generatorOptions:
  disableNameSuffixHash: true
