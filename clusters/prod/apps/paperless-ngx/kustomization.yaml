resources:
  - release.yaml

configMapGenerator:
  - name: app-values
    files:
      - app-values.yaml=app-values.yaml
  - name: data-values
    files:
      - data-values.yaml=data-values.yaml

generatorOptions:
  disableNameSuffixHash: true
