apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: plex
  namespace: default

spec:
  interval: 1h0m0s
  releaseName: plex

  chart:
    spec:
      chart: unichart
      version: "0.0.14"
      sourceRef:
        kind: HelmRepository
        name: tinycloud-labs
        namespace: flux-system
      interval: 2h0m0s

  valuesFrom:
    - kind: ConfigMap
      name: plex-values
      valuesKey: values.yaml
