apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: exacalidraw
spec:
  values:
    app:
      image:
        tag: latest
      replicaCount: 1
    ingress:
      enabled: true
      host: draw.shakir.cloud
      ingressClassName: nginx
