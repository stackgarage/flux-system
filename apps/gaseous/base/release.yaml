apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: gaseous
  namespace: default

spec:
  interval: 1h0m0s
  releaseName: gaseous

  chart:
    spec:
      chart: unichart
      version: "0.0.11"
      sourceRef:
        kind: HelmRepository
        name: tinycloud-labs
        namespace: flux-system
      interval: 2h0m0s

  valuesFrom:
    - kind: ConfigMap
      name: gaseous-values
      valuesKey: values.yaml
