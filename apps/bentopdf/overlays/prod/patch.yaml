apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: bentopdf
spec:
  values:
    app:
      image:
        tag: sha-370968e
      replicaCount: 1
    ingress:
      enabled: true
      host: bentopdf.shakir.cloud
      ingressClassName: traefik

