app:
  name: music-assistant
  image:
    repository: ghcr.io/music-assistant/server
    pullPolicy: IfNotPresent
    tag: latest
  service:
    port: 80
    targetPort: 6595

  volumeMounts:
    - mountPath: /data
      subPath: data

  env:
    - name: LOG_LEVEL
      value: info
